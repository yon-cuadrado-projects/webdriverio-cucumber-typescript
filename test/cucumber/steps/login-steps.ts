import { Then, When } from '@cucumber/cucumber';
import { BasePage } from '../pages/common/BasePage';

When( 'The user sets the user {string} in the {string} page', function ( string, string2 ) {
  // Write code here that turns the phrase above into concrete actions
  return 'pending';
} );

When( 'The user sets the password {string} in the {string} page', function ( string, string2 ) {
  // Write code here that turns the phrase above into concrete actions
  return 'pending';
} );

Then( 'The message {string} appears', function ( string ) {
  // Write code here that turns the phrase above into concrete actions
  return 'pending';
} );
